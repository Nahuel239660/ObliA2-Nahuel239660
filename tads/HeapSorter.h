#ifndef HEAPSORTER_H
#define HEAPSORTER_H

class HeapSorter {
public:
    // pre: arr no es null, n > 0
    // post: ordena el array de menor a mayor usando heapsort
    static void sort(int* arr, int n);
};

#endif
